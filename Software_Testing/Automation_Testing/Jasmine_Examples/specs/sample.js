describe('suite', () => {
    it('should do X', () => {
      expect(true).toBe(true)
    })
    it('should do X', () => {
      expect(false).toBe(true)
    })
    it('should do X', () => {
      expect(false).toBe(alse)
    })
  })